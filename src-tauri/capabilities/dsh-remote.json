{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dsh-remote",
  "description": "Remote dsh Web UI origin (loopback only): expose nothing but the zoom command",
  "windows": ["main"],
  "remote": { "urls": ["http://127.0.0.1:*"] },
  "permissions": ["allow-zoom-ui"]
}
